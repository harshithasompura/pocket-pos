import { ReceiptText } from "lucide-react-native";
import { FoundationScreen } from "@/src/features/foundation/foundation-screen";
export const SellScreen = () => <FoundationScreen icon={ReceiptText} title="Ready for the billing flow" body="Your offline business and product foundation is ready. Cart and receipt workflows are the next phase." />;
export default SellScreen;
